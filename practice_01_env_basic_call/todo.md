# 实操 1 准备事项

本文件仅记录课前需要准备的材料。README 默认这些内容已经准备完毕。

## 模型

- 本地视觉语言模型，例如 Qwen2.5-VL、Qwen-VL、InternVL、LLaVA。
- 默认路径：`../models/vlm`。

## 数据

- 测试图片：`../data/images/demo.jpg`。

## 环境依赖

- Python 运行环境。
- 可用 GPU 与 CUDA 环境。
- Python 包见 `requirements.txt`。

## 课前检查

```bash
ls ../models/vlm
ls ../data/images/demo.jpg
sed -n '1,120p' requirements.txt
```

